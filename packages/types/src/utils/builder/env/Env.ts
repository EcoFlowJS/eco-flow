/**
 *  This code snippet is defining an interface named `Environment`. The interface
 * `Environment` has two properties: `name` of type `string` and `value` of type `string`.
 */
export interface Environment {
  name: string;
  value: string;
}
